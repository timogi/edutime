---
alwaysApply: true
description: "Always use @gluestack-ui/themed components instead of React Native core components where possible. Prefer components like Box, Text, Button, etc. from @gluestack-ui/themed over View, Text, TouchableOpacity from react-native. Exception: Use custom toast component instead of Gluestack toast as it doesn't always work reliably."
---

# Gluestack UI Rules for EduTime Native

## 🚫 Never Do This
```typescript
// ❌ Using React Native core components when Gluestack equivalents exist
import { View, Text, TouchableOpacity } from 'react-native';

<View>
  <TouchableOpacity onPress={handlePress}>
    <Text>Button</Text>
  </TouchableOpacity>
</View>

// ❌ Using Gluestack toast (unreliable)
import { useToast } from '@gluestack-ui/themed';
const toast = useToast();
toast.show({ title: 'Success' });
```

## ✅ Always Do This
```typescript
// ✅ Use Gluestack UI components
import { Box, Text, Button } from '@gluestack-ui/themed';

<Box>
  <Button onPress={handlePress}>
    <ButtonText>Button</ButtonText>
  </Button>
</Box>

// ✅ Use custom toast component (since Gluestack toast doesn't always work)
import { showToast } from '@/components/ui/Toast';
showToast({ type: 'success', title: 'Success', message: 'Operation completed' });
```

## 📁 Files to Always Touch for Gluestack UI

### 1. **Gluestack Configuration**
- `gluestack-ui.config.ts` - Gluestack configuration
- `tailwind.config.js` - Tailwind integration
- `components/ui/` - Custom UI components

### 2. **Component Files** (When using UI components)
- `app/**/*.tsx` - All app screens
- `components/**/*.tsx` - All components
- `components/RecordForm/**/*.tsx` - Form components
- `components/settings/**/*.tsx` - Settings components

## 🏗️ Gluestack UI Patterns

### 1. **Component Replacement**
```typescript
// ✅ Replace React Native components with Gluestack equivalents
// View → Box
<Box style={styles.container}>
  <Text>Content</Text>
</Box>

// TouchableOpacity → Pressable
<Pressable onPress={handlePress}>
  <Text>Pressable</Text>
</Pressable>

// Text → Text (from Gluestack)
<Text size="md" color="$primary500">Styled Text</Text>
```

### 2. **Custom Toast Component**
```typescript
// ✅ Create custom toast component (since Gluestack toast is unreliable)
import Toast from 'react-native-toast-message';

interface ToastProps {
  type: 'success' | 'error' | 'info' | 'warning';
  title: string;
  message?: string;
  duration?: number;
}

export const showToast = ({ type, title, message, duration = 3000 }: ToastProps) => {
  Toast.show({
    type,
    text1: title,
    text2: message,
    visibilityTime: duration,
    position: 'bottom',
  });
};

// Usage
showToast({
  type: 'success',
  title: 'Success',
  message: 'Record saved successfully'
});
```

### 3. **Gluestack Component Usage**
```typescript
// ✅ Use Gluestack components with proper props
import { Button, ButtonText, VStack, HStack, Card, Text } from '@gluestack-ui/themed';

<VStack space="md" style={styles.container}>
  <Card style={styles.card}>
    <Text size="lg" fontWeight="$bold">Title</Text>
    <Text size="md" color="$gray600">Description</Text>
  </Card>
  
  <HStack space="sm">
    <Button variant="solid" size="md" onPress={handleSave}>
      <ButtonText>Save</ButtonText>
    </Button>
    <Button variant="outline" size="md" onPress={handleCancel}>
      <ButtonText>Cancel</ButtonText>
    </Button>
  </HStack>
</VStack>
```

## 🎯 Common Gluestack Patterns

### 1. **Layout Components**
```typescript
// ✅ Use VStack and HStack for layouts
<VStack space="md" style={styles.container}>
  <HStack space="sm" alignItems="center">
    <Text>Label:</Text>
    <Text fontWeight="$bold">Value</Text>
  </HStack>
</VStack>
```

### 2. **Button Components**
```typescript
// ✅ Use Button with proper variants
<Button variant="solid" size="md" onPress={handlePress}>
  <ButtonText>Primary Button</ButtonText>
</Button>

<Button variant="outline" size="sm" onPress={handleSecondary}>
  <ButtonText>Secondary Button</ButtonText>
</Button>
```

### 3. **Card Components**
```typescript
// ✅ Use Card for content containers
<Card style={styles.card}>
  <VStack space="sm">
    <Text size="lg" fontWeight="$bold">Card Title</Text>
    <Text size="md">Card content goes here</Text>
  </VStack>
</Card>
```

## ⚠️ Critical Rules

1. **Use Gluestack components** - Prefer over React Native core components
2. **Exception: Use custom toast** - Gluestack toast doesn't always work
3. **Follow Gluestack patterns** - Use proper component props and styling
4. **Test across platforms** - Ensure Gluestack components work on iOS/Android
5. **Use design system** - Leverage Gluestack's theming system

## 🔍 Files That Need Gluestack Attention

Based on your codebase, these files commonly need Gluestack updates:
- `components/RecordList.tsx` - Replace View with Box
- `components/RecordForm/RecordForm.tsx` - Use Gluestack components
- `app/(app)/index.tsx` - Button and layout components
- `components/settings/*.tsx` - Form components
- `components/WeekdayPicker.tsx` - Card and layout components

## 🚀 Quick Checklist

When using Gluestack UI:
- [ ] Use Box instead of View
- [ ] Use Pressable instead of TouchableOpacity
- [ ] Use Button with proper variants
- [ ] Use VStack/HStack for layouts
- [ ] Use Card for content containers
- [ ] Use custom toast component (not Gluestack toast)
- [ ] Test on both iOS and Android
- [ ] Follow Gluestack design patterns
